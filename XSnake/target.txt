- 每个用户有独立分数并且都要显示
- 用户暂停时该蛇停止，变成墙
- 有食物(蛋)/炸弹/障碍物，都可以继承一个方格类（还可以有的东西：传送黑洞；隐藏草丛（蛋不会生成在草丛里）
- 游戏开始前三秒倒计时

- //如果要使两只蛇可以不同速率运行的话需要开三个线程，一个给渲染，另外两个给蛇
- //用户一方调按键即可，两方调参需按Ctrl并且需征得同意(是否需要同意也可以设置boolean 
- 按P为游戏暂停，按空格为一方暂停，按e为游戏结束(若一方死亡也会结束游戏)
- 可以选择蛇撞墙以后是眩晕几秒还是直接死亡)

- 同步方式=>1.每一次传输命令都需要确认并返回，非客户端那一段收到Beat消息以后再更新 （目前打算用这种方法，首先更改：客户端的刷新由服务器tick完成)
  	    2.每次只管发送命令不check，更新的时候再修正。